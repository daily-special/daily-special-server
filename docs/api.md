# 런타임 API — daily-special-server

> 클라이언트가 이 서버와 말하는 **유일한 표면**. **원본은 이 저장소가 소유한다.**
>
> `data-contract.md`와 헷갈리지 말 것. 그쪽은 **파이프라인 산출물**(손님·재료·요리·대사)의
> 계약이고, 5절이 "런타임 상태는 여기 넣지 않는다"고 못박는다. 이 문서는 그 반대편 —
> **플레이 중에 바뀌는 것**을 다루는 표면이다.
>
> 이 문서는 자체 완결로 쓴다. 외부 문서를 참조하지 않는다.
> 최종 수정: 2026-08-06 · 아래 예시는 전부 **실제 응답을 받아 적은 것**이다

---

## 1. 표기

- **키는 `snake_case`.** 데이터 계약과 같은 모양을 쓴다 — 세 저장소가 표기를 통일하는 편이 싸다
- **열거 값은 소문자 `snake_case`.** 서버 내부 `enum`은 자바 관례대로 대문자지만 **그 이름은 API로 새지 않는다.** API 층이 변환한다
- 시간·날짜를 응답에 담지 않는다. 게임 내 날짜는 요청이 정한다

## 2. 버전

경로에 박는다. `/api/v1/...`

major가 바뀌면 경로가 바뀐다. 필드를 **더하는** 것은 major를 올리지 않으니, 클라이언트는 **모르는 필드를 무시**해야 한다.

## 3. 자물쇠

**`X-Api-Key` 헤더가 필요하다.** 없거나 틀리면 `401 unauthorized`.

```
curl -H "X-Api-Key: ..." http://<host>/api/v1/...
```

`/actuator/health`만 키 없이 열린다 — 배포 확인에 키가 필요하면 곤란해서다.

**이것은 인증이 아니라 자물쇠다.** 누가 부르는지 구별하지 않으므로, 키를 가진 사람은 아무 `save_id`나 조회하고 바꿀 수 있다. **여전히 남은 빚이고**, 플레이어 식별이 붙을 때 갚는다.

서버가 키 없이 뜨면(환경변수를 비우면) 필터가 꺼진다. 로컬과 테스트가 그 상태로 돈다.

---

## 4. `GET /api/v1/saves/{save_id}/days/{day_number}/guests/{guest_id}/visit-state`

손님이 **오늘 어떤 상태로 오는가**를 돌려준다.

**읽기 전용이고 부수 효과가 없다.** 같은 경로는 언제 불러도 같은 답을 낸다 — 상태를 저장하지 않고 씨앗에서 다시 계산하기 때문이다. 재시도해도 안전하고, 캐시해도 안전하다.

### 경로 변수

| | 타입 | 설명 |
|---|---|---|
| `save_id` | string | 세이브 식별자. 비어 있으면 400 |
| `day_number` | integer | 게임 내 날짜. **1부터.** 0 이하면 400 |
| `guest_id` | string | 데이터 계약의 손님 슬러그 |

### 200

```json
{
  "save_id": "save-1",
  "day_number": 1,
  "guest_id": "guest_dusty_patrol_01",
  "hunger": 67,
  "condition": "normal",
  "mood": "elated",
  "wallet": 24,
  "needs": ["special", "filling"]
}
```

| 필드 | 타입 | 값 |
|---|---|---|
| `hunger` | integer | 0~100 |
| `condition` | string | `normal` · `injured` · `tired` |
| `mood` | string | `gloomy` · `calm` · `elated` |
| `wallet` | integer | 오늘 이 손님이 쓸 수 있는 돈 |
| `needs` | string[] | 이번 방문의 **우선 욕구**. 1~2개, 중요한 순서다. `filling` · `restorative` · `mild` · `stimulating` · `affordable` · `special` |

요청 식별자 셋을 **되돌려 담는다.** 응답만 보고도 무엇에 대한 답인지 알 수 있게 하려는 것이다.

**응답에 없는 것**: 이름·소개·말투·취향 구간·식이 제약. 그것들은 안 바뀌므로 데이터 계약이 소유하고, **클라이언트가 자기 번들에서 `guest_id`로 읽는다.** 서버는 중계하지 않는다.

### 오류

모든 실패가 같은 모양으로 나간다. `error`는 기계가 분기할 값이고 `message`는 사람이 읽을 설명이다. **`message`에 기대어 분기하지 말 것** — 문구는 바뀐다.

| 상태 | `error` | 언제 |
|---|---|---|
| 404 | `unknown_guest` | 서버 스냅샷에 없는 `guest_id` |
| 400 | `invalid_request` | 0 이하 날짜, 숫자가 아닌 날짜, 빈 식별자 |

```json
{ "error": "unknown_guest", "message": "모르는 손님이다: guest_ashen_scout_01" }
```

### ⚠️ 404가 뜨면 대개 콘텐츠 버전 문제다

서버는 **파이프라인 `guests.json` 스냅샷**을 자기 리소스로 들고 있다. 클라이언트가 번들한 것과 **다른 스냅샷**이면 `guest_id`가 하나도 안 맞아 전부 404가 된다.

실제로 한 번 겪었다 — 클라가 손으로 쓴 목 데이터(4명)를 들고 있고 서버는 생성분(8명)을 들고 있어서, 두 집합의 겹침이 0이었다. **둘 다 `schema_version`이 1.0.0이라 형식 검사로는 안 잡힌다.**

**양쪽이 같은 산출물을 봐야 한다.** 콘텐츠를 다시 뽑으면 서버 리소스와 클라 번들을 같이 갱신한다.

---

## 5. 관계

### `GET /api/v1/saves/{save_id}/guests/{guest_id}/relationship`

```json
{
  "save_id": "save-1",
  "guest_id": "guest_dusty_patrol_01",
  "affinity": 36,
  "tier": "familiar",
  "disclosed": {
    "preferred_needs": true,
    "dietary": false,
    "all_axes": false,
    "axes": ["seasoning"]
  }
}
```

첫 방문 전이라도 **404가 아니다.** 관계가 없는 것은 `affinity: 0`, `tier: "stranger"`다.

| 필드 | 값 |
|---|---|
| `affinity` | 0~100 |
| `tier` | `stranger` · `familiar` · `regular` |
| `disclosed.*` | **무엇이 열렸는가.** 값이 아니다 |

### ⭐ 응답에 취향 값이 실리지 않는다

**서버는 "열렸는지"만 말한다.** 취향 구간·식이 제약·선호 욕구는 안 바뀌므로 데이터 계약이 소유하고 **클라이언트가 자기 번들에서 읽는다.** 서버가 그것을 배달하면 숨은 페르소나가 네트워크로 나가고, 서버가 고정 콘텐츠를 들고 있어야 한다.

`all_axes`가 참이면 클라이언트는 `axes`를 무시하고 모든 축을 연다 — 그래서 **서버는 축 어휘를 몰라도 된다.**

### `POST /api/v1/saves/{save_id}/guests/{guest_id}/visits`

방문 하나를 기록한다. 요청은 **클라이언트가 계산한 것**이다.

```json
{ "satisfaction": 0.82, "off_axes": ["seasoning"] }
```

| 필드 | 값 |
|---|---|
| `satisfaction` | 0~1. 클라이언트 만족도 엔진의 결과. 필수 |
| `off_axes` | 이상 구간을 벗어난 축. 없으면 생략 가능 |

응답은 갱신된 관계로, `GET`과 같은 모양이다.

**만족도를 서버가 다시 계산하지 않는다.** 엔진은 클라이언트가 소유한다 — 서버가 같이 가지면 두 벌이 되고 두 벌은 갈라진다.

**⚠️ 멱등하지 않다.** 같은 요청을 두 번 보내면 관계가 두 번 자란다. 하루에 한 번만 부르는 것은 지금 클라이언트의 책임이다. 재시도 안전이 필요해지면 날짜를 키로 받아 막는다.

### 오류

| 상태 | `error` | 언제 |
|---|---|---|
| 404 | `unknown_guest` | 콘텐츠 스냅샷에 없는 `guest_id` |
| 400 | `invalid_request` | 만족도가 0~1 밖이거나 빠졌을 때, 본문을 읽을 수 없을 때 |

---

## 5. 아직 없는 것

- **재고 · 소지금·평판 · 시장 시세** — 전부 서버 소유지만 아직 화면이 요구하지 않는다
- **손님 여러 명을 한 번에** — 지금은 한 명씩. 하루 손님 목록이 생기면 묶는다
- **인증** — 3절
